# Autogenerated file using backend module.
terraform {
  
  # Required version.
  required_version = "~> 1.4.0"

  # Required providers.
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.0"
    }
  }

  # Configure backend. 
  backend "s3" {
    bucket               = "${bucket_name}"
    key                  = "terraform.tfstate"
    region               = "${aws_region}"
    encrypt              = true
    kms_key_id           = "${kms_key}"
    dynamodb_table       = "${dynamodb_table}"
    workspace_key_prefix = "${project}/workspace"
  }
  
}
